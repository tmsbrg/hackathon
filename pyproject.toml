[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "doc-triage"
version = "0.1.0"
description = "Local document triage CLI for high-value findings."
requires-python = ">=3.12"

[project.scripts]
doc-triage = "doc_triage.cli:main"
